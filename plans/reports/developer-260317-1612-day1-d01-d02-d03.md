# Day 1 Sprint Report — D01 / D02 / D03

**Status: PASS**
**Date:** 2026-03-17
**Project:** `/Users/hung/Desktop/Project/portfolio_tracker`

---

## Tasks Completed

- [x] TASK-D01: Flutter project created, dependencies installed, folder structure created, all core files written, `flutter analyze` passes with zero issues
- [x] TASK-D02: `Asset` and `Portfolio` Freezed models created, `build_runner` generated `.freezed.dart` and `.g.dart` files, `flutter analyze` zero issues
- [x] TASK-D03: `AssetRepository` abstract class + `MockAssetRepository` implementation created, `flutter analyze` zero issues

---

## Files Created (with line counts)

| File | Lines |
|------|-------|
| `lib/main.dart` | 7 |
| `lib/app/app.dart` | 25 |
| `lib/app/router.dart` | 15 |
| `lib/core/constants/app_colors.dart` | 14 |
| `lib/core/constants/app_text_styles.dart` | 33 |
| `lib/core/constants/app_spacing.dart` | 8 |
| `lib/core/errors/failures.dart` | 15 |
| `lib/core/utils/currency_formatter.dart` | 17 |
| `lib/core/utils/percent_formatter.dart` | 8 |
| `lib/features/portfolio/data/models/asset.dart` | 37 |
| `lib/features/portfolio/data/models/portfolio.dart` | 15 |
| `lib/features/portfolio/data/repositories/asset_repository.dart` | 14 |
| `lib/features/portfolio/data/repositories/mock_asset_repository.dart` | 54 |
| `test/widget_test.dart` (updated) | 14 |

All files under 200 lines. No magic numbers — all spacing/color values use `AppSpacing`/`AppColors` constants. No `print()` statements.

---

## flutter analyze Output

```
Analyzing portfolio_tracker...
No issues found! (ran in 1.0s)
```

---

## Tests Status

- `flutter pub get`: exit 0
- `dart run build_runner build --delete-conflicting-outputs`: exit 0, 4 outputs written (asset.freezed.dart, asset.g.dart, portfolio.freezed.dart, combining builder output)
- `flutter analyze`: zero issues
- `flutter test`: `00:00 +1: All tests passed!`

---

## Issues Encountered and Resolved

1. **`prefer_const_constructors` lint** in `lib/app/app.dart` — `ColorScheme.dark(...)` was not marked `const`. Fixed by adding `const` keyword.

2. **Stale `widget_test.dart`** — default scaffold referenced `MyApp` (old generated class). Replaced with test that pumps `PortfolioTrackerApp` wrapped in `ProviderScope`.

3. **Wrong test assertion** — initial test used `findsNothing` for `MaterialApp`, but `MaterialApp.router` still produces a `MaterialApp` node. Fixed assertion to `findsOneWidget`.

---

## Folder Structure Created

```
lib/
├── app/
│   ├── app.dart
│   └── router.dart
├── core/
│   ├── constants/
│   │   ├── app_colors.dart
│   │   ├── app_text_styles.dart
│   │   └── app_spacing.dart
│   ├── errors/
│   │   └── failures.dart
│   └── utils/
│       ├── currency_formatter.dart
│       └── percent_formatter.dart
├── features/
│   └── portfolio/
│       ├── data/
│       │   ├── models/
│       │   │   ├── asset.dart
│       │   │   ├── asset.freezed.dart (generated)
│       │   │   ├── asset.g.dart (generated)
│       │   │   ├── portfolio.dart
│       │   │   └── portfolio.freezed.dart (generated)
│       │   └── repositories/
│       │       ├── asset_repository.dart
│       │       └── mock_asset_repository.dart
│       ├── domain/
│       │   └── providers/
│       └── presentation/
│           ├── screens/
│           └── widgets/
└── shared/
    └── widgets/
```
